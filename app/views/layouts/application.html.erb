<!DOCTYPE html>
<html>
<head>
  <title>MyProject</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
</head>
<body>
  <nav>
    <% if logged_in? %>
      <p>
        Merhaba, <%= current_user.email %> |
        <%= link_to "Profilim", profile_path(current_user) %> |
        
        <%= link_to "Çıkış Yap", logout_path, method: :delete %>
      </p>
    <% else %>
      <%= link_to "Giriş Yap", login_path %> |
      <%= link_to "Kayıt Ol", new_user_path %>
    <% end %>
  </nav>

  <%= yield %>
</body>
</html>