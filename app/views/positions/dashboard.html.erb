<!DOCTYPE html>
<html>
  <head>
    <title>Şirket Profili</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Google Fonts - Orbitron -->
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;600;700&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <style>
      body {
        margin: 0;
        padding: 0;
        background: #0D0C18;
        color: #fff;
        font-family: system-ui, -apple-system, sans-serif;
      }

      /* Sidebar Styles */
      .sidebar {
        position: fixed;
        left: 0;
        top: 0;
        width: 70px;
        height: 100vh;
        background: rgba(13, 12, 24, 0.95);
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        padding: 1.5rem 0.5rem;
        border-right: 1px solid rgba(139, 92, 246, 0.1);
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        z-index: 1000;
        overflow: hidden;
        box-shadow: 0 0 30px rgba(0, 0, 0, 0.2);
      }

      .sidebar:hover {
        width: 240px;
      }

      /* Logo Styles */
      .logo {
        padding: 0.75rem;
        margin-bottom: 2rem;
        position: relative;
      }

      .logo-container {
        display: flex;
        align-items: center;
        gap: 0.5rem;
      }

      .logo i {
        font-size: 2rem;
        color: #8B5CF6;
        filter: drop-shadow(0 0 8px rgba(139, 92, 246, 0.5));
      }

      .logo span {
        font-family: 'Orbitron', sans-serif;
        font-size: 1.4rem;
        font-weight: 600;
        color: #8B5CF6;
        opacity: 0;
        transform: translateX(-10px);
        transition: all 0.3s ease;
      }

      .sidebar:hover .logo span {
        opacity: 1;
        transform: translateX(0);
      }

      /* Menu Styles */
      .menu-label {
        padding: 0.5rem 0.75rem;
        font-size: 0.75rem;
        color: rgba(255,255,255,0.6);
        opacity: 0;
        transition: opacity 0.3s ease;
      }

      .sidebar:hover .menu-label {
        opacity: 1;
      }

      .nav-menu {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
      }

      .nav-item {
        display: flex;
        align-items: center;
        gap: 1rem;
        padding: 0.75rem;
        color: rgba(255,255,255,0.8);
        text-decoration: none;
        border-radius: 8px;
        transition: all 0.3s ease;
      }

      .nav-item i {
        font-size: 1.25rem;
        min-width: 24px;
      }

      .nav-item span {
        font-size: 0.95rem;
        opacity: 0;
        transform: translateX(-10px);
        transition: all 0.3s ease;
      }

      .sidebar:hover .nav-item span {
        opacity: 1;
        transform: translateX(0);
      }

      .nav-item:hover, .nav-item.active {
        background: rgba(139, 92, 246, 0.2);
        color: #8B5CF6;
      }

      /* Main Content */
      main {
        margin-left: 70px;
        min-height: 100vh;
        transition: margin-left 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        background: #0D0C18;
      }

      .sidebar:hover + main {
        margin-left: 240px;
      }

      /* Dashboard Styles */
      .dashboard-header {
        background: rgba(255, 255, 255, 0.02);
        border-bottom: 1px solid rgba(139, 92, 246, 0.1);
        padding: 1.5rem 0;
        margin-bottom: 2rem;
      }

      .company-info {
        display: flex;
        align-items: center;
        gap: 1.5rem;
      }

      .company-logo {
        width: 80px;
        height: 80px;
        border-radius: 12px;
        overflow: hidden;
        border: 2px solid rgba(139, 92, 246, 0.2);
      }

      .company-logo img {
        width: 100%;
        height: 100%;
        object-fit: cover;
      }

      .company-details h1 {
        margin: 0;
        font-size: 1.8rem;
        font-weight: 600;
      }

      .company-details p {
        margin: 0.5rem 0 0;
        color: rgba(255, 255, 255, 0.6);
      }

      .dashboard-card {
        background: rgba(255, 255, 255, 0.02);
        border: 1px solid rgba(139, 92, 246, 0.2);
        border-radius: 12px;
        padding: 1.5rem;
        height: 100%;
        transition: all 0.3s ease;
      }

      .dashboard-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
      }

      .dashboard-card h3 {
        font-size: 1.2rem;
        margin-bottom: 1rem;
        color: rgba(255, 255, 255, 0.9);
      }

      .stat-card {
        background: rgba(139, 92, 246, 0.1);
        border-radius: 8px;
        padding: 1rem;
        text-align: center;
      }

      .stat-card h4 {
        font-size: 2rem;
        margin: 0;
        color: #8B5CF6;
      }

      .stat-card p {
        margin: 0.5rem 0 0;
        color: rgba(255, 255, 255, 0.6);
        font-size: 0.9rem;
      }

      .job-list {
        list-style: none;
        padding: 0;
        margin: 0;
      }

      .job-item {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 1rem;
        border-bottom: 1px solid rgba(139, 92, 246, 0.1);
      }

      .job-item:last-child {
        border-bottom: none;
      }

      .job-title {
        color: rgba(255, 255, 255, 0.9);
        text-decoration: none;
        transition: color 0.3s ease;
      }

      .job-title:hover {
        color: #8B5CF6;
      }

      .job-meta {
        display: flex;
        align-items: center;
        gap: 1rem;
        color: rgba(255, 255, 255, 0.6);
        font-size: 0.9rem;
      }

      .btn-primary {
        background: #8B5CF6;
        border: none;
        padding: 0.75rem 1.5rem;
        font-weight: 500;
      }

      .btn-primary:hover {
        background: #7C3AED;
      }

      /* Button Styles */
      .btn-group {
        display: flex;
        gap: 0.5rem;
      }

      .btn {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.75rem 1.5rem;
        font-weight: 500;
        border-radius: 8px;
        transition: all 0.3s ease;
      }

      .btn-primary {
        background: #8B5CF6;
        border: none;
      }

      .btn-primary:hover {
        background: #7C3AED;
      }

      .btn-outline-light {
        border: 1px solid rgba(255, 255, 255, 0.2);
        color: #fff;
      }

      .btn-outline-light:hover {
        background: rgba(255, 255, 255, 0.1);
        border-color: rgba(255, 255, 255, 0.3);
        color: #fff;
      }

      /* Responsive */
      @media (max-width: 768px) {
        .sidebar {
          width: 70px;
        }
        
        .sidebar:hover {
          width: 70px;
        }
        
        .logo span,
        .menu-label,
        .nav-item span {
          display: none;
        }
        
        main {
          margin-left: 70px !important;
        }

        .company-info {
          flex-direction: column;
          text-align: center;
        }

        .company-details h1 {
          font-size: 1.5rem;
        }
      }

      /* Table Styles */
      .table {
        border-radius: 12px;
        overflow: hidden;
        margin-bottom: 0;
      }

      .table th {
        background: rgba(139, 92, 246, 0.1);
        border-bottom: 1px solid rgba(139, 92, 246, 0.2);
        color: #8B5CF6;
        font-weight: 500;
        padding: 1rem;
      }

      .table td {
        padding: 1rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        vertical-align: middle;
      }

      .table tbody tr:hover {
        background: rgba(139, 92, 246, 0.05);
      }

      .table tbody tr:last-child td {
        border-bottom: none;
      }

      /* Button Styles */
      .btn-outline-primary {
        border-color: rgba(139, 92, 246, 0.3);
        color: #8B5CF6;
      }

      .btn-outline-primary:hover {
        background: rgba(139, 92, 246, 0.1);
        border-color: #8B5CF6;
        color: #8B5CF6;
        transform: translateY(-1px);
      }

      .btn-outline-success {
        border-color: rgba(16, 185, 129, 0.3);
        color: #10B981;
      }

      .btn-outline-success:hover {
        background: rgba(16, 185, 129, 0.1);
        border-color: #10B981;
        color: #10B981;
        transform: translateY(-1px);
      }

      .position-card {
        background: rgba(255, 255, 255, 0.02);
        border: 1px solid rgba(139, 92, 246, 0.1);
        transition: all 0.3s ease;
      }

      .position-card:hover {
        border-color: rgba(139, 92, 246, 0.2);
        box-shadow: 0 8px 32px rgba(139, 92, 246, 0.1);
      }

      .applications-section {
        background: rgba(13, 12, 24, 0.5);
        border-radius: 12px;
        padding: 1.5rem;
        border: 1px solid rgba(139, 92, 246, 0.1);
      }
    </style>
  </head>

  <body>
    <%= render 'shared/flash' %>
    
    <!-- Sidebar -->
    <div class="sidebar">
      <div class="logo">
        <div class="logo-container">
          <i class="bi bi-gem-fill"></i>
          <span>Rubytes</span>
        </div>
      </div>
      
      <nav class="nav-menu">
        <div class="menu-label">MENU</div>
        
        <%= link_to root_path, class: "nav-item" do %>
          <i class="bi bi-columns-gap"></i>
          <span>Dashboard</span>
        <% end %>
        
        <%= link_to edit_company_registration_path, class: "nav-item" do %>
          <i class="bi bi-person-badge"></i>
          <span>Profil</span>
        <% end %>
        
        <%= link_to "#", class: "nav-item" do %>
          <i class="bi bi-stars"></i>
          <span>Rozetler</span>
        <% end %>
        
        <%= link_to "#", class: "nav-item" do %>
          <i class="bi bi-chat-square-heart"></i>
          <span>Forum</span>
        <% end %>

        <%= link_to "#", class: "nav-item" do %>
          <i class="bi bi-calendar-event"></i>
          <span>Etkinlikler</span>
        <% end %>

        <%= link_to positions_path, class: "nav-item active" do %>
          <i class="bi bi-briefcase"></i>
          <span>İş İlanları</span>
        <% end %>

        <%= link_to cv_path, class: "nav-item" do %>
          <i class="bi bi-file-earmark-person"></i>
          <span>CV</span>
        <% end %>
        
        <%= link_to "#", class: "nav-item" do %>
          <i class="bi bi-robot"></i>
          <span>AI Chat</span>
        <% end %>

        <div class="nav-footer">
          <%= link_to "#", class: "nav-item logout-btn" do %>
            <i class="bi bi-box-arrow-right"></i>
            <span>Çıkış Yap</span>
          <% end %>
        </div>
      </nav>
    </div>

    <!-- Main Content -->
    <main>
      <!-- Dashboard Header -->
      <div class="dashboard-header">
        <div class="container">
          <div class="d-flex justify-content-between align-items-center">
            <div class="company-info">
              <div class="company-logo">
                <% if current_company.logo.attached? %>
                  <%= image_tag current_company.logo %>
                <% else %>
                  <div class="bg-dark d-flex align-items-center justify-content-center h-100">
                    <i class="bi bi-building text-primary" style="font-size: 2rem;"></i>
                  </div>
                <% end %>
              </div>
              <div class="company-details">
                <h1><%= current_company.name %></h1>
                <p><%= current_company.email %></p>
              </div>
            </div>
            <div class="btn-group">
              <%= link_to edit_company_registration_path, class: "btn btn-outline-light" do %>
                <i class="bi bi-pencil-square me-2"></i>
                Profili Düzenle
              <% end %>
              <%= link_to new_position_path, class: "btn btn-primary" do %>
                <i class="bi bi-plus-lg"></i>
                <span>Yeni İlan Oluştur</span>
              <% end %>
            </div>
          </div>
        </div>
      </div>

      <div class="container">
        <!-- Stats Row -->
        <div class="row g-4 mb-4">
          <div class="col-md-4">
            <div class="dashboard-card">
              <div class="stat-card">
                <h4><%= @positions.count %></h4>
                <p>Aktif İlan</p>
              </div>
            </div>
          </div>
          <div class="col-md-4">
            <div class="dashboard-card">
              <div class="stat-card">
                <h4><%= @total_applications %></h4>
                <p>Toplam Başvuru</p>
              </div>
            </div>
          </div>
          <div class="col-md-4">
            <div class="dashboard-card">
              <div class="stat-card">
                <h4><%= @total_views %></h4>
                <p>Toplam Görüntülenme</p>
              </div>
            </div>
          </div>
        </div>

        <!-- İlanlarım Section -->
        <div class="card bg-dark border-0 rounded-4 mb-4 position-relative overflow-hidden">
          <div class="card-header border-0 bg-transparent">
            <h3 class="text-white mb-0 d-flex align-items-center gap-2">
              <i class="bi bi-briefcase text-primary"></i>
              İlanlarım
            </h3>
          </div>
          <div class="card-body">
            <% @positions.each do |position| %>
              <div class="position-card bg-dark-subtle p-4 rounded-4 mb-4">
                <div class="d-flex justify-content-between align-items-start mb-4">
                  <div>
                    <h4 class="text-white mb-2"><%= position.title %></h4>
                    <div class="text-white-50">
                      <i class="bi bi-calendar me-2"></i><%= position.application_deadline&.strftime("%d.%m.%Y") %>
                      <i class="bi bi-eye ms-3 me-2"></i><%= position.views_count || 0 %> görüntülenme
                      <i class="bi bi-person ms-3 me-2"></i><%= position.applications.count %> başvuru
                    </div>
                  </div>
                  <div class="d-flex gap-2">
                    <%= link_to edit_position_path(position), class: "btn btn-outline-light btn-sm" do %>
                      <i class="bi bi-pencil"></i>
                    <% end %>
                    <%= button_to position_path(position), method: :delete, class: "btn btn-outline-danger btn-sm", data: { confirm: "Bu ilanı silmek istediğinize emin misiniz?" } do %>
                      <i class="bi bi-trash"></i>
                    <% end %>
                  </div>
                </div>

                <!-- Başvuranlar Section -->
                <div class="applications-section mt-4">
                  <h5 class="text-primary mb-3">
                    <i class="bi bi-people me-2"></i>
                    Başvuranlar
                  </h5>
                  
                  <% if position.applications.any? %>
                    <div class="table-responsive">
                      <table class="table table-dark table-hover">
                        <thead>
                          <tr>
                            <th>İsim</th>
                            <th>Email</th>
                            <th>Başvuru Tarihi</th>
                            <th>CV</th>
                            <th>İşlemler</th>
                          </tr>
                        </thead>
                        <tbody>
                          <% position.applications.each do |application| %>
                            <tr>
                              <td class="text-white"><%= application.user.name %></td>
                              <td class="text-white-50"><%= application.user.email %></td>
                              <td class="text-white-50"><%= application.created_at.strftime("%d.%m.%Y %H:%M") %></td>
                              <td>
                                <% if application.cv.attached? %>
                                  <%= link_to rails_blob_path(application.cv, disposition: "attachment"), class: "btn btn-outline-primary btn-sm" do %>
                                    <i class="bi bi-download me-1"></i>CV İndir
                                  <% end %>
                                <% else %>
                                  <span class="text-muted">CV Yok</span>
                                <% end %>
                              </td>
                              <td>
                                <div class="btn-group">
                                  <%= link_to "#", class: "btn btn-outline-success btn-sm", title: "Kabul Et" do %>
                                    <i class="bi bi-check-lg"></i>
                                  <% end %>
                                  <%= link_to "#", class: "btn btn-outline-danger btn-sm", title: "Reddet" do %>
                                    <i class="bi bi-x-lg"></i>
                                  <% end %>
                                </div>
                              </td>
                            </tr>
                          <% end %>
                        </tbody>
                      </table>
                    </div>
                  <% else %>
                    <div class="text-center text-white-50 py-4">
                      <i class="bi bi-inbox display-4 d-block mb-3"></i>
                      <p class="mb-0">Henüz başvuru bulunmuyor.</p>
                    </div>
                  <% end %>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </main>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html> 