<!DOCTYPE html>
<html>
  <head>
    <title>Yeni İlan Oluştur</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Google Fonts - Orbitron -->
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;600;700&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <style>
      body {
        margin: 0;
        padding: 0;
        background: #0D0C18;
        color: #fff;
        font-family: system-ui, -apple-system, sans-serif;
      }

      /* Form Card Styles */
      .form-card {
        background: rgba(13, 12, 24, 0.95);
        border-radius: 24px;
        border: 1px solid rgba(139, 92, 246, 0.1);
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        padding: 2.5rem;
        position: relative;
        z-index: 1;
        overflow: hidden;
      }

      .form-card::before {
        content: '';
        position: absolute;
        top: -2px;
        left: -2px;
        right: -2px;
        bottom: -2px;
        background: linear-gradient(
          45deg,
          #8B5CF6,
          #C850C0,
          #4158D0,
          #8B5CF6
        );
        background-size: 400% 400%;
        z-index: -1;
        border-radius: 25px;
        animation: borderRotate 3s ease infinite;
      }

      .form-card::after {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: rgba(13, 12, 24, 0.95);
        border-radius: 24px;
        z-index: -1;
      }

      @keyframes borderRotate {
        0% {
          background-position: 0% 50%;
        }
        50% {
          background-position: 100% 50%;
        }
        100% {
          background-position: 0% 50%;
        }
      }

      /* Form Title */
      .form-title {
        font-family: 'Orbitron', sans-serif;
        font-size: 2rem;
        font-weight: 600;
        text-align: center;
        margin-bottom: 2rem;
        color: #8B5CF6;
        text-shadow: 0 0 10px rgba(139, 92, 246, 0.3);
      }

      /* Form Controls */
      .form-label {
        color: rgba(255, 255, 255, 0.9);
        font-weight: 500;
        margin-bottom: 0.5rem;
      }

      .form-control {
        background: rgba(255, 255, 255, 0.03) !important;
        border: 1px solid rgba(139, 92, 246, 0.2) !important;
        border-radius: 12px !important;
        color: #fff !important;
        padding: 0.75rem 1rem;
        transition: all 0.3s ease;
      }

      .form-control:focus {
        box-shadow: 0 0 0 4px rgba(139, 92, 246, 0.1) !important;
        border-color: rgba(139, 92, 246, 0.4) !important;
      }

      .form-control::placeholder {
        color: rgba(255, 255, 255, 0.4);
      }

      /* Select Input */
      .form-select {
        background: rgba(255, 255, 255, 0.03) !important;
        border: 1px solid rgba(139, 92, 246, 0.2) !important;
        border-radius: 12px !important;
        color: #fff !important;
        padding: 0.75rem 1rem;
        cursor: pointer;
        transition: all 0.3s ease;
      }

      .form-select:focus {
        box-shadow: 0 0 0 4px rgba(139, 92, 246, 0.1) !important;
        border-color: rgba(139, 92, 246, 0.4) !important;
      }

      .form-select option {
        background: #1a1a2e;
        color: #fff;
      }

      /* File Input */
      .file-input-wrapper {
        position: relative;
        overflow: hidden;
        display: inline-block;
        width: 100%;
      }

      .file-input-button {
        background: rgba(139, 92, 246, 0.1);
        border: 1px solid rgba(139, 92, 246, 0.2);
        border-radius: 12px;
        color: #fff;
        padding: 0.75rem 1rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        cursor: pointer;
        transition: all 0.3s ease;
      }

      .file-input-button:hover {
        background: rgba(139, 92, 246, 0.15);
        border-color: rgba(139, 92, 246, 0.3);
      }

      .file-input-button i {
        font-size: 1.2rem;
        color: #8B5CF6;
      }

      /* Form Buttons */
      .btn-primary {
        background: linear-gradient(135deg, #8B5CF6, #7C3AED);
        border: none;
        padding: 0.75rem 1.5rem;
        border-radius: 12px;
        font-weight: 500;
        transition: all 0.3s ease;
      }

      .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 20px rgba(139, 92, 246, 0.3);
      }

      .btn-outline-light {
        background: rgba(255, 255, 255, 0.03);
        border: 1px solid rgba(139, 92, 246, 0.2);
        padding: 0.75rem 1.5rem;
        border-radius: 12px;
        font-weight: 500;
        transition: all 0.3s ease;
      }

      .btn-outline-light:hover {
        background: rgba(139, 92, 246, 0.1);
        border-color: rgba(139, 92, 246, 0.3);
        color: #8B5CF6;
        transform: translateY(-2px);
      }

      /* Form Help Text */
      .form-text {
        color: rgba(255, 255, 255, 0.5);
        font-size: 0.875rem;
        margin-top: 0.5rem;
      }

      /* Responsive */
      @media (max-width: 768px) {
        .form-card {
          padding: 1.5rem;
        }
      }
    </style>
  </head>

  <body>
    <%= render 'shared/flash' %>

    <div class="container py-5">
      <div class="row justify-content-center">
        <div class="col-lg-8">
          <div class="form-card">
            <h1 class="form-title">Yeni İlan Oluştur</h1>

            <%= form_with(model: @position, local: true, class: "position-form") do |f| %>
              <div class="mb-4">
                <%= f.label :title, "Başlık", class: "form-label" %>
                <%= f.text_field :title, class: "form-control", placeholder: "Örn: Senior Frontend Developer" %>
              </div>

              <div class="mb-4">
                <%= f.label :description, "Açıklama", class: "form-label" %>
                <%= f.text_area :description, class: "form-control", rows: 6, placeholder: "İş tanımı ve gereksinimleri..." %>
              </div>

              <div class="mb-4">
                <%= f.label :category, "İş Türü", class: "form-label" %>
                <%= f.select :category, ["Full Time", "Part Time", "Internship"], {}, class: "form-select" %>
              </div>

              <div class="mb-4">
                <%= f.label :application_deadline, "Son Başvuru Tarihi", class: "form-label" %>
                <%= f.date_field :application_deadline, class: "form-control" %>
              </div>

              <div class="mb-4">
                <label class="form-label">İlan Görseli</label>
                <div class="file-input-wrapper">
                  <label class="file-input-button">
                    <i class="bi bi-cloud-upload"></i>
                    <span>Dosya Seç</span>
                    <%= f.file_field :image, class: "d-none", accept: "image/png,image/jpeg,image/jpg" %>
                  </label>
                </div>
                <div class="form-text">PNG, JPEG veya JPG formatında, maksimum 5MB</div>
              </div>

              <div class="d-grid gap-2">
                <%= f.submit "İlan Oluştur", class: "btn btn-primary" %>
                <%= link_to "Vazgeç", positions_path, class: "btn btn-outline-light" %>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
  