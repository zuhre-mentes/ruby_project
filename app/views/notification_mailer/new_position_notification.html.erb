<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    <style>
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333;
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .header {
        background: linear-gradient(135deg, #8B5CF6, #7C3AED);
        color: white;
        padding: 20px;
        border-radius: 10px 10px 0 0;
        text-align: center;
      }
      .content {
        background: #f9fafb;
        padding: 20px;
        border-radius: 0 0 10px 10px;
        border: 1px solid #e5e7eb;
      }
      .button {
        display: inline-block;
        background: #8B5CF6;
        color: white;
        padding: 12px 24px;
        text-decoration: none;
        border-radius: 6px;
        margin-top: 20px;
      }
      .company-info {
        margin: 20px 0;
        padding: 15px;
        background: white;
        border-radius: 8px;
        border: 1px solid #e5e7eb;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <h1>New Job Listing</h1>
    </div>
    
    <div class="content">
      <p>Hello <%= @user.email %>,</p>
      
      <p><strong><%= @company.name %></strong> has posted a new job listing!</p>
      
      <div class="company-info">
        <h2><%= @position.title %></h2>
        <p><strong>Company:</strong> <%= @company.name %></p>
        <p><strong>Position Type:</strong> <%= @position.category %></p>
        <p><strong>Application Deadline:</strong> <%= @position.application_deadline&.strftime("%d.%m.%Y") %></p>
        
        <p><%= truncate(@position.description, length: 200) %></p>
      </div>
      
      <%= link_to "View Listing", position_url(@position), class: "button" %>
      
      <p style="margin-top: 30px; font-size: 14px; color: #666;">
        You are receiving this email from TechHunt.
      </p>
    </div>
  </body>
</html>
